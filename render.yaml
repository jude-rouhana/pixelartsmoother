services:
  - type: web
    name: pixel-art-smoother
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_backend.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
